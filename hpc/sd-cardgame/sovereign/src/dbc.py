#/usr/bin/env python

import sys, os
import itertools
import json
import argparse


class Sovereign(object):
    def __init__(self):
        self.play


    def load_cards():
        with open(jsonfile) as card_file:
            cards = json.load(card_file)
            
        return cards


